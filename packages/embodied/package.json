{
	"name": "@lv-robotics/pi-embodied",
	"version": "0.0.1",
	"private": true,
	"description": "Embodied-agent harness on pi: robot tools over the robot service RPC, batch evaluation",
	"type": "module",
	"scripts": {
		"eval": "node --import tsx src/eval.ts",
		"test": "vitest --run",
		"check": "tsgo --noEmit -p tsconfig.json"
	},
	"peerDependencies": {
		"@earendil-works/pi-ai": "*",
		"@earendil-works/pi-coding-agent": "*",
		"typebox": "*"
	}
}
